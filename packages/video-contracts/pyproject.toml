[project]
name = "daily-line-video-contracts"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "jsonschema[format]>=4.23,<5",
  "referencing>=0.35,<1"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
